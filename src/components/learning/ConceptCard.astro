---
interface Props {
  title: string;
  open?: boolean;
}
const { title, open = false } = Astro.props;
---

<details class="concept-card" open={open}>
  <summary>{title}</summary>
  <div class="concept-card-body">
    <slot />
  </div>
</details>
